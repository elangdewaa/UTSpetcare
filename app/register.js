import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const register = (hatiku) => {
  return (
    <View>
      <Text>register</Text>
    </View>
  )
}

export default register

const styles = StyleSheet.create({})